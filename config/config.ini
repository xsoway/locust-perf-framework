[runtime]
# 被压测服务根地址；CI 或本地临时执行时可用 LOCUST_TARGET_HOST 覆盖。
host = https://example.com
environment = local
# 图片/文件流式读取超时，设为 0 表示不设超时。可用 LOCUST_REQUEST_TIMEOUT_SECONDS 临时覆盖。
request_timeout_seconds = 100
wait_time_min_seconds = 1
wait_time_max_seconds = 3

[logging]
level = INFO
log_dir = logs
