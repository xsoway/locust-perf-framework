# ===========================================================================
# test-locust-platform 环境变量示例
# 复制本文件为 .env 或直接在 CI 中注入同名变量。所有值均为示例占位。
# 切勿把真实密钥/网关地址写入仓库。
# ===========================================================================

# --- 被压测服务 ----------------------------------------------------------
# 默认从 config/config.ini 的 host 读取；设置本变量可临时覆盖。
# LOCUST_TARGET_HOST=https://your-service.example.com

# --- 可选：LLM 辅助分析（报告分析 Agent 调用）-------------------------------
# AiApiClient 是 OpenAI 兼容网关封装，以下三项都需要时才启用分析；
# 不设置则报告跳过 LLM 摘要（或使用 --no-llm-analysis 显式跳过）。
# AI_APICLIENT_BASE_URL=https://your-llm-gateway.example.com/v1
# AI_APICLIENT_API_KEY=sk-your-key
# AI_APICLIENT_MODEL=your-model-name
# 可选：透传给网关的额外请求头（JSON 字符串）
# AI_APICLIENT_EXTRA_HEADERS={"c1":"qa","c2":"test"}