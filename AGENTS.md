# AGENTS.md · locust-perf-framework Agent 规则入口

<!-- code_project: parent-agents-required -->

## 父级规则继承（强制）

无论通过 Codex、Claude Code 或 OpenCode 打开本项目，开始任何任务前都必须依次读取并遵循：`../../AGENTS.md`、`../AGENTS.md`、本文件。规则优先级固定为：用户当前明确要求 > 工作区根 `AGENTS.md` > `src/AGENTS.md` > 本文件的项目局部规则。局部规则只能补充，不能放宽或重排父级规则。


## 项目定位

本项目是 Python + Locust 通用性能测试框架，目标是沉淀可复用的压测方案设计、脚本开发、执行记录、报告分析与 LLM 辅助总结能力。

## 分层边界

- `config/`: 环境配置、LLM 调用配置、压测参数模板。
- `data/`: 测试数据，禁止写死在 Locust 脚本中。
- `locustfiles/`: 独立存放 Locust 压测脚本。
- `reports/raw/`: Locust 原始产物，例如 CSV、HTML、日志摘录。
- `reports/html/`: 二次分析后的 HTML 报告。
- `reports/analysis/`: LLM 分析结果、Markdown/JSON 中间产物。
- `logs/`: 框架运行日志和压测辅助日志。
- `tools/`: 自定义工具、命名、日志、报告生成、执行封装。
- `agents/`: 规划 Agent、脚本开发 Agent、报告分析 Agent 的职责和入口。

## 命名规则

- Locust 脚本命名：`locust_<业务域>_<接口或场景>_<测试类型>.py`。
- 测试数据命名：`<业务域>_<接口或场景>_<用途>.csv|json|yaml`。
- 原始报告目录：`reports/raw/<场景名>/<YYYYMMDD_HHMMSS>/`。
- 分析报告文件：`reports/html/<场景名>_<测试类型>_<YYYYMMDD_HHMMSS>.html`。
- 日志文件：`logs/<模块名>_<YYYYMMDD>.log`。

## 不覆盖规则

- 每次执行必须生成新的时间戳目录或文件名。
- 禁止重跑时覆盖已有 `reports/`、`logs/`、`data/` 产物。
- 如检测到目标路径已存在，必须自动追加序号或重新生成时间戳。

## 命令封装规则

- 面向用户的压测执行命令必须优先封装到 `tools/`，只暴露必要参数，例如并发数、启动速率、运行时长、目标环境、数据文件。
- 不直接向用户提供需要手动拼接时间戳、创建目录、指定 CSV/HTML 路径、设置多组环境变量的复杂命令。
- 执行入口必须自动生成时间戳目录、Locust 原始产物、失败明细和最终中文报告。
- README 和操作说明中优先展示一体化封装命令；底层 Locust 命令仅作为调试或排障补充。

## Python + Locust 开发规范

- 公共模块、类、函数必须有中文 docstring。
- 关键压测参数、等待时间、数据读取、断言逻辑必须有简洁中文注释。
- 禁止在脚本中硬编码 token、cookie、账号密码、生产域名密钥。
- 正式运行路径必须使用 `tools.logging_setup.get_logger()` 记录日志。
- Locust task 中必须明确接口名称、请求方法、校验点和失败记录。
- 测试数据必须从 `data/` 读取，脚本只描述行为，不承载大段数据。
- 报告必须包含执行背景、压测方案、关键指标、异常摘要、结论与建议。

## Agent 职责

- 规划 Agent：先向用户确认业务背景、接口信息、压测目标、测试类型、并发模型、指标口径、风险和退出条件。
- 脚本开发 Agent：根据确认后的方案开发 Locust 脚本、数据文件、运行配置，并保证命名和日志规范。
- 报告分析 Agent：读取 Locust 原始报告和日志，调用 `config/ai_apiclient.py` 辅助分析，输出带时间戳的 HTML 报告。

## 测试类型参考

- 基准测试：建立性能基线，典型场景是新系统上线前的单接口摸底。
- 负载测试：验证正常负载下的性能，典型场景是预期日活 10000 用户的系统。
- 压力测试：找到系统崩溃点，典型场景是逐步加压直到系统不可用。
- 稳定性测试：验证长时间运行稳定性，典型场景是 48 小时持续压测并观察内存泄漏。
- 峰值测试：验证突发流量表现，典型场景是秒杀活动开始瞬间。

## 不要做的事

- 不把接口业务规则写进框架核心。
- 不在单元测试里真实调用 LLM 或真实业务接口。
- 不覆盖历史报告。
- 不把报告结论只写成指标堆砌，必须给出可决策的风险与建议。
- 不把复杂底层命令直接交给用户执行，除非用户明确要求查看或调试底层命令。
